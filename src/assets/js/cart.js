document.querySelectorAll('.qty-input').forEach(input=>{input.addEventListener('change',()=>{const data=new FormData();data.append('id',input.dataset.id);data.append('quantity',input.value);fetch('index.php?page=update-cart',{method:'POST',body:data}).then(r=>r.json()).then(d=>{const fmt=n=>'Rp '+Number(n).toLocaleString('id-ID');document.getElementById('subtotal-'+input.dataset.id).innerText=fmt(d.subtotal);document.getElementById('cart-total').innerText=fmt(d.total);});});});
